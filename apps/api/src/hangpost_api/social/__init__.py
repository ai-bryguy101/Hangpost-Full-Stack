"""Social graph domain.

Owns ``friendships`` (pending/accepted/blocked edges) and
``friendship_imports`` (provenance + consent hash for imported edges —
the GDPR signal called out in CLAUDE.md §4).
"""
