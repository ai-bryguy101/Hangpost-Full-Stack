# Recommendation ranker evaluation — SYNTHETIC self-test

> **Synthetic data, real code.** Generated by feeding a seeded synthetic
> impression/outcome set through the shipping
> `hangpost_api.recommendations.evaluation` module — the same functions
> `scripts/evaluate.py` runs on live data. It exists to prove the metrics
> and baselines work and to show the report format; it is **not** a
> measurement of real users. The first real report lands once outcomes
> accrue (run `python -m scripts.evaluate` in the Codespace).

- Generated: 2026-05-29 (synthetic)
- Queries: **30** · candidates/query: **10**

| Method | NDCG@10 | Recall@10 |
|---|---|---|
| `live_ranker` | 0.9285 | 1.0000 |
| `popularity` | 0.5837 | 1.0000 |
| `random` | 0.5655 | 1.0000 |

Relevance grading: `friend_request_sent` / `hangout_rsvped` = 3, `profile_opened` = 2, `viewed` = 1, none = 0.

_Synthetic self-test generated from the real evaluation module._
